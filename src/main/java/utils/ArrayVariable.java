package utils;

/**
 * @author nivanov
 *         on 12.12.16.
 */
public class ArrayVariable extends Variable {
    public ArrayVariable(String name, Class type, Object value, Boolean immutable) {
        super(name, type, value, immutable);
    }
}
